/* const client = require('../app/controller/controllerCliente'); 

test('Añade un usuario y se espera que el servicio devuelva un boolean True', async () => {
    const req = 
    {
        body: { 
            nombre_cliente: "Alex Gruñon", 
            password: "pinchealex", 
            cedula: 1123415, 
            numero_personal: 12331231423, 
            correo_personal: "asadq@gmail.com" 
        }
    };
    const cliente = await client.addCliente(req);
    console.log(cliente); 
    expect(cliente).toBe(true);
  }); */